{
  "//": "Solution style root. Each package type checks itself via its own tsconfig; this file exists so an editor opening the repository root resolves the packages.",
  "files": [],
  "include": [],
  "references": [
    { "path": "./packages/api-client" },
    { "path": "./packages/config" },
    { "path": "./packages/auth" }
  ]
}
